# Bootstrap + PXE Server Inventory
# ansible-runner runs inside a container, so we must target the host by IP
# (not localhost). bootstrap.sh updates the IP before running the playbook.

[bootstrap_servers]
local-bootstrap ansible_host=__BOOTSTRAP_IP__
